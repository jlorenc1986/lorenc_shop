/* @jsx REACT.DOM */

var Navbar = React.createClass({

        render: function() {


            return <nav>
            navbar
            </nav>
        }
    });

    parent = document.getElementById("navbar");

React.render(<Navbar/>, parent);
